# DO NOT EDIT. This file is generated by `devctl gen circleci` and kept in sync
# by the giantswarm/github align-files workflow. Change the generator in devctl
# (pkg/gen/input/circleci) or the gen.ci config in giantswarm/github, not here.
# Repo-specific jobs and workflows go in .circleci/custom.yml, which the setup
# workflow in .circleci/config.yml merges into this file at pipeline runtime.
#
# explicit_allow_chart_name_mismatch is set on the push jobs because the chart
# name (kagent) differs from the repo name (kagent-app): this repo follows the
# Giant Swarm app-repo `-app` naming, while the chart must not end in `-app`.
version: 2.1
orbs:
  architect: giantswarm/architect@9.4.3

workflows:
  build:
    jobs:

    # build-chart: package + lint via app-build-suite and persist the archive
    # (no push). Shared by the branch and tag paths -- branches get build + test
    # only by default.
    - architect/push-to-app-catalog:
        executor: app-build-suite
        context: architect
        name: build-chart
        app_catalog: giantswarm-catalog
        app_catalog_test: giantswarm-test-catalog
        chart: kagent
        explicit_allow_chart_name_mismatch: true
        push_to_appcatalog: false
        push_to_oci_registry: false
        persist_chart_archive: true
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore:
            - main

    # Branch: run chart tests after build-chart.
    - architect/run-tests-with-ats:
        name: execute-chart-tests
        requires:
        - build-chart
        filters:
          branches:
            ignore:
            - main

    # Tag: run chart tests after build.
    - architect/run-tests-with-ats:
        name: execute-chart-tests-release
        requires:
        - build-chart
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/

    # Tag: push chart after tests. Intentionally does NOT depend on
    # sync-china-registry so the chart catalog can publish even if the in-China
    # Aliyun mirror is slow or fails.
    - architect/push-to-app-catalog:
        executor: app-build-suite
        context: architect
        name: push-chart-release
        app_catalog: giantswarm-catalog
        app_catalog_test: giantswarm-test-catalog
        chart: kagent
        explicit_allow_chart_name_mismatch: true
        requires:
        - execute-chart-tests-release
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/
